# 第四节课学员项目交付说明

交付日期：2026-07-24

## 交付文件

- `ff-companybrain.zip`：学员项目源码、Docker Compose、部署说明和脱敏课堂数据。
- `ff-companybrain.zip.sha256`：压缩包完整性校验值。
- `ff-companybrain/`：与压缩包内容一致的可编辑副本。

## 学员启动

解压后进入项目根目录：

```sh
cp deploy/compose/.env.example deploy/compose/.env
chmod 600 deploy/compose/.env
# 填写自己的密码、Embedding Key、Agent Key 和至少 32 位的内部 Token
deploy/compose/student-start.sh
```

浏览器访问 `http://127.0.0.1:3000`。详细说明见项目内 `README.md`。

## 数据说明

- 保留课堂所需的已入库测试数据。
- 历史登录会话已删除。
- 测试账号为 `student01` 至 `student15`，统一密码 `student123456`。
- 管理员密码在首次导入时绑定为学员 `.env` 中的 `ADMIN_PASSWORD`。
- 交付包不包含讲师 `.env`、API Key 或数据库密码。

## 发布前验证结果

- Docker 源码构建：通过。
- PostgreSQL 6 库恢复：通过。
- Neo4j 图数据恢复：通过，37 个节点、34 条关系。
- 业务测试数据：10 个场景、17 组问答历史、4 个 Traditional RAG 文档、16 个 GraphRAG 文档。
- 管理员登录：通过。
- 学员测试账号登录：通过。
- 二次启动保留已有数据：通过。
- 快照 SHA-256、ZIP 完整性、敏感值和缓存/测试残留检查：通过。

源项目 `/Users/mac/Git/ff-companybrain` 未作为清理目标；交付整理只发生在第四节课复制副本中。
